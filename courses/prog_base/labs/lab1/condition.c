#include <math.h>

int satisfies(int a, int b, int c)
{

    int result, modmin, max, min;


    if (a<0 && b<0 && c<0)
    {
        modmin = abs(min(a,b,c);
                     if (min(a,b,c)=a)
    {
        sum2 = b+c
    };
    if (min(a,b,c)=b)
    {
        sum2 = a+c
    };
    if (min(a,b,c)=c)
    {
        sum2 = b+a
    };
    if (sum2<=-256 & modmin > 0 && (a & (a - 1)) = 0)
    {
        result = True
    };
    else if (abs(sum2)-modmin <16||abs(sum2)<16)
    {
        result = True
    }
};
if (a<0,b>0,c>0||b<0,c>0,a>0||c<0,a>0,b>0)
    {
        if (a<0)
        {
            if (a>-256)
            {
                result = True
            }
        };
        if (b<0)
        {
            if (b>-256)
            {
                result = True
            }
        };
        if (c<0)
        {
            if (c>-256)
            {
                result = True
            }
        };
        if (a<0 & b<0 || a<0 & c<0 || c <0 & b<0)
        {
            if (a<0 & b<0)
            {
                if ((a+b)*4>-256)
                {
                    result == True
                }
            };
            if (b<0 & c<0)
            {
                if ((b+c)*4>-256)
                {
                    result = True
                }
            };
            if (a<0 & c<0)
            {
                if ((a+c)*4>-256)
                {
                    result = True
                }
            }
        };
        if (a>=o & b>=0 & c>=0)
        {
            max = max(a,b,c);
            min = min(a,b,c);
            if (max%min == 0 & max!=0)
            {
                result = True
            };
            if (min = 0)
            {
                result - False
            };
            if (max = 0 & min!=0)
            {
                result = True
            }
        }



        return result;
    }
